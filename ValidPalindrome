class Solution {
    public boolean isPalindrome(String s) {
        String result = s.replaceAll("[^a-zA-Z0-9]", "");
        String sb= new StringBuilder(result).reverse().toString();
        return (result.equalsIgnoreCase(sb));
    }
}
