class Solution {
    public boolean isThree(int n) {
      int k=0;
      for (int i = 1; i <= Math.abs(n); i++) {
            if (n % i == 0) {
               k++;
            }
        }
    return k==3;  
    }
}
